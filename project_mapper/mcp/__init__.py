"""
project_mapper.mcp
MCP (Model Context Protocol) interface: the stdio JSON-RPC server (`server`)
and the tool schemas + handlers (`tools`).

A thin adapter over project_mapper.core — it formats engine results for AI
agents and never contains analysis logic itself.
"""
