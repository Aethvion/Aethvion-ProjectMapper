"""
project_mapper.http
HTTP (FastAPI) interface: the app factory + pm-server CLI (`app`) and the REST
routes (`routes`).

A thin adapter over project_mapper.core. Kept so the Aethvion Suite and hosted
deployments (Railway, Smithery uptime) can reach the engine over HTTP.
"""
